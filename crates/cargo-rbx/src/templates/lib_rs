extern crate rb_sys;

use rb_sys::*;
use std::ffi::{{CString}};

#[allow(non_snake_case)]
#[no_mangle]
pub extern "C" fn Init_{name}() {{
    let module_name = CString::new("{name}").unwrap();

    unsafe {{
        let klass = rb_define_module(module_name.as_ptr());
    }}
}}
